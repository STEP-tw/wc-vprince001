* Add .gitignore
* Make directory structure

* node ./wc.js file1
    * make test for top level function
    * take a single file as input
* Review/Refactor code
* Add library for constants
* divide functions into smaller functions if possible

* Sepearte util tests to util_test file

* Add parser to parse single option -l for line count
* Modify parser to handle -c and -w option